<li>
    <div class="collapsible-header">
        <i class="material-icons">attach_money</i>
        A Receber Vencidos
        <span class="badge"><%= (current_user.admin?) ? Card.not_received.count : Card.not_received.profissional.count %></span>
    </div>

    <div class="collapsible-body">
        <div class="row">
            <% if current_user.admin? %>
                <% Card.not_received.each do |card| %>
                    <%= render 'to_do_cards', card: card %>
                <% end %>
            <% else %>
                <% Card.not_received.profissional.each do |card| %>
                    <%= render 'to_do_cards', card: card %>
                <% end %>
            <% end %>
        </div>
    </div>
</li>