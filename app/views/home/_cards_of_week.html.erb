<li>
    <div class="collapsible-header">
        <i class="material-icons">content_paste</i>
        Lembretes da semana
        <span class="badge"><%= Card.to_do.count %></span>
    </div>

    <div class="collapsible-body">
        <div class="row">
            <% Card.to_do.each do |card| %>
                <%= render 'to_do_cards', card: card %>
            <% end %>
        </div>
    </div>
</li>